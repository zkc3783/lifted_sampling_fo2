

uv run wfoms -i ./models/stable_roommates/stm_4_20.wfomcs -a incremental3 -s 1000
uv run wfoms -i ./models/stable_roommates/stm_4_24.wfomcs -a incremental3 -s 1000
uv run wfoms -i ./models/stable_roommates/stm_4_28.wfomcs -a incremental3 -s 1000
uv run wfoms -i ./models/stable_roommates/stm_4_32.wfomcs -a incremental3 -s 1000
uv run wfoms -i ./models/stable_roommates/stm_4_36.wfomcs -a incremental3 -s 1000
uv run wfoms -i ./models/stable_roommates/stm_4_40.wfomcs -a incremental3 -s 1000
uv run wfoms -i ./models/stable_roommates/stm_4_44.wfomcs -a incremental3 -s 1000
uv run wfoms -i ./models/stable_roommates/stm_4_48.wfomcs -a incremental3 -s 1000

uv run wfoms -i ./models/stable_roommates/stm_5_12.wfomcs -a incremental3 -s 1000
uv run wfoms -i ./models/stable_roommates/stm_5_16.wfomcs -a incremental3 -s 1000
uv run wfoms -i ./models/stable_roommates/stm_5_20.wfomcs -a incremental3 -s 1000
uv run wfoms -i ./models/stable_roommates/stm_5_24.wfomcs -a incremental3 -s 1000
uv run wfoms -i ./models/stable_roommates/stm_5_28.wfomcs -a incremental3 -s 1000
uv run wfoms -i ./models/stable_roommates/stm_5_32.wfomcs -a incremental3 -s 1000


cd lifted_sampling_fo2-master/models/stable_roommates
uv run python more_efficient_stable_roommates.py
uv run python stable_roommates_marriages_cnf.py




uv run wfoms -i ./models/regular_graphs/2-regular-graph-sc2.wfomcs -a incremental3 -s 3
uv run wfoms -i ./models/regular_graphs/2-regular-graph.wfomcs -a incremental3 -s 3
uv run wfoms -i ./models/existential.wfomcs -a incremental3 -s 3
uv run wfoms -i ./models/31.wfomcs -a incremental3 -s 3

uv run sampler -i ./models/stable_roommates/stm_4_20.wfomcs -o ./models/stable_roommates/results  -k 1 -s True




#vscode windows默认终端改为cmd
#打开并搜索设置： terminal default profile 然后修改Default Profile: Windows 的设置项


#Conda 把“默认 env 存放目录”写在了.condarc配置项 envs_dirs 里，改这一项即可每次create不手动指定位置
#没有则新建
dir C:\Users\zkc90\.condarc


#手动修改
envs_dirs:
  - D:/Anaconda/envs 


#保存立即生效，查看效果，修改的应为第一个，在默认的之前
conda config --show envs_dirs


#Conda 会查找列表中的第一个 可写路径 来创建新环境，注意权限
conda  create -n wfomc2

# 进入项目根目录（setup.py 同级）
cd /path/to/project

# 开发模式（可编辑，代码改完立即生效）
pip install -e .

#（可选）额外补依赖，若项目还带了 requirements.txt，运行：
pip install -r requirements.txt



#windows powershell代理
$env:HTTP_PROXY="http://127.0.0.1:7890"
$env:HTTPS_PROXY="http://127.0.0.1:7890"

#windows cmd代理
set HTTP_PROXY=http://127.0.0.1:7890
set HTTPS_PROXY=http://127.0.0.1:7890


cmd2==2.5.11
pynauty无windows版本无法运行Recursive

copilot
输入以使用Copilot CLI


WSL nat模式
windows：172.26.112.1
linux：172.26.115.228

git status
git config --global user.name "zkc3783"
git config --global user.email "zkc9090@icloud.com"

git config --global http.proxy http://172.26.115.228:7890
git config --global https.proxy http://172.26.115.228:7890
git config --global --unset http.proxy
git config --global --unset https.proxy

git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/zkc3783/lifted_sampling_fo2.git